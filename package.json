{
  "name": "loxhuebridge-mehrlampensteuerung",
  "version": "2.5.12-dev",
  "description": "Philips Hue V2 to Loxone Bridge with grouped per-light multi-sync, group effect fallback and robust SSE parsing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --experimental-test-coverage test/*.test.js"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "mqtt": "^5.15.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
